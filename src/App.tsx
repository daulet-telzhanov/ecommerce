import React, { FC } from 'react';
import './App.scss';

const App: FC = () => {
    const timer = 0;

    return (
        <div className="app">
            {timer}
        </div>
    );
};

export default App;
